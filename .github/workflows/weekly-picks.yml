name: Weekly Picks & Ops

on:
  workflow_dispatch:

jobs:
  sanity:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Hello
        run: echo "✅ Actions is working for ${{ github.actor }} on ${{ github.ref }}"
